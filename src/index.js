import React from 'react'
import ReactDOM from 'react-dom/client'

import NewTaskForm from './components/new-task-form/new-task-form'

ReactDOM.createRoot(document.querySelector('.todoapp')).render(<NewTaskForm />)
